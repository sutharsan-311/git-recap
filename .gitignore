# Dependencies are intentionally zero. Do not add any.
node_modules/
demo-repo/
*/git-recap/
# ...and the same thing when the CLI is run from the repo root with no --out,
# which the nested pattern above does not match.
/git-recap/
*.log
.DS_Store

# Machine-local Claude Code settings
.claude/settings.local.json

# Local strategy notes, not for publication
docs/MONETIZATION.md
